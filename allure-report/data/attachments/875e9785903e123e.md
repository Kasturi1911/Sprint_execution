# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: negative.spec.ts >> Parabank Negative UI Scenarios >> Login Negative Scenarios >> Empty Password Validation
- Location: tests\negative.spec.ts:27:1

# Error details

```
Error: locator.clear: Target page, context or browser has been closed
Call log:
  - waiting for getByLabel('Username')

```

# Test source

```ts
  1  | import { expect, Page } from '@playwright/test';
  2  | 
  3  | import { Logger } from '../Utils/logger';
  4  | 
  5  | export class LoginPage {
  6  | 
  7  |   constructor(private page: Page) {}
  8  | 
  9  |   async login(username: string, password: string) {
  10 |   Logger.info('Entering Login Credentials');
  11 |   await this.page.getByLabel('Username').fill(username);
  12 |   await this.page.getByLabel('Password').fill(password);
  13 |   Logger.info('Clicking Login Button');
  14 |   await this.page.locator('input[value="Log In"]').click();
  15 |   await expect(this.page.getByRole('heading', { name: 'Accounts Overview' })).toBeVisible({ timeout: 15000 }); // increased from default 5000
  16 |   Logger.info('Login Successful');
  17 | }
  18 | 
  19 |  async attemptLogin(username: string, password: string) {
  20 |   Logger.info(`Attempting login with username: ${username}`);
> 21 |    await this.page.getByLabel('Username').clear();
     |                                           ^ Error: locator.clear: Target page, context or browser has been closed
  22 |   await this.page.getByLabel('Username').fill(username);
  23 |   await this.page.getByLabel('Password').clear();
  24 |   await this.page.getByLabel('Password').fill(password);
  25 |   await this.page.getByRole('button', { name: 'Log In' }).click();
  26 |   await this.page.waitForLoadState('domcontentloaded');
  27 | }
  28 | }
```